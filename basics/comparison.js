// console.log("-02" > 1);

console.log(null > 0);
console.log(null == 0);
console.log(null >= 0);
console.log(null === 0);

// null comparison is a bit tricky, undefined will always give false, since its undefined.

// tripple equality => strict check => checks the value as well as the data type.

